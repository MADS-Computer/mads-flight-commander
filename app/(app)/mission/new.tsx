// Metro picks MissionBuilder.native.tsx on iOS/Android (interactive map + form)
// and MissionBuilder.tsx on web (name + drone assignment only).
export { default } from '@/components/mission/MissionBuilder';
